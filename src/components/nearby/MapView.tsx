const MapView = () => {
  return (
    <div className="h-full w-full rounded-xl bg-gray-800 flex items-center justify-center text-muted-foreground">
      <p>Mapa (pins aproximados) aparecerá aqui.</p>
    </div>
  );
};

export default MapView;
